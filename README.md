# Ollie Ebook Shelf

目前主路徑：

- `/bookshelf/`：書櫃首頁
- `/bookshelf/spider/`：蜘蛛英雄那本
- `/bookshelf/hulk/`：浩克刷牙那本

英文命名：

- `bookshelf` = 書櫃

相容舊路徑：

- `/` 會轉到 `/bookshelf/`
- `/spider/` 會轉到 `/bookshelf/spider/`
- `/hulk/` 會轉到 `/bookshelf/hulk/`
- `/books/spider/` 會轉到 `/bookshelf/spider/`
- `/books/hulk/` 會轉到 `/bookshelf/hulk/`
- `/ebook/` 會轉到 `/bookshelf/`
- `/ebook/spider/` 會轉到 `/bookshelf/spider/`
- `/ebook/hulk/` 會轉到 `/bookshelf/hulk/`
